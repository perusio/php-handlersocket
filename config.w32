// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("handlersocket", "for handlersocket support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("handlersocket", "enable handlersocket support", "no");

if (PHP_HANDLERSOCKET != "no") {
	EXTENSION("handlersocket", "handlersocket.cc");
}

